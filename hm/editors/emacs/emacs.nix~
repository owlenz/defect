{ pkgs, self, ... }:
{
  programs.emacs.enable = true;
  services.emacs = {
    enable = true;
    package = pkgs.emacs-pgtk;
    startWithUserSession = "graphical";
    defaultEditor = true;
  };
  xdg.configFile."emacs".source = "${self}/config/emacs";
}
